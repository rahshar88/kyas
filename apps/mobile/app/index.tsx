// Route files are one-line re-exports. Everything under `app/` is a route to Expo Router,
// so screens and their tests live under `src/features/*` (spec §6.2).
export { LaunchScreen as default } from '@/features/launch/screens/LaunchScreen';
