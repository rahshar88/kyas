# supabase/migrations

Empty on purpose. Milestone 1 creates the first migration (profiles, invites, verification_requests).

Spec §11.3: "Do not create empty future tables merely to appear complete. Add them with
the milestone that owns them."
