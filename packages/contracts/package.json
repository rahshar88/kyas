{
  "name": "@kyascene/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "KyaScene API request/response contracts shared by the mobile app, admin console and Edge Functions.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@kyascene/domain": "workspace:*"
  },
  "scripts": {
    "test": "echo \"no tests in @kyascene/contracts yet\" && exit 0",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@kyascene/config": "workspace:*"
  }
}
