{
  "name": "@kyascene/ui",
  "version": "0.1.0",
  "private": true,
  "description": "KyaScene design tokens and the shared mobile component library.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "peerDependencies": {
    "react": "*",
    "react-native": "*",
    "react-native-safe-area-context": "*"
  },
  "scripts": {
    "test": "echo \"@kyascene/ui tests run through the mobile jest projects\" && exit 0",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@kyascene/config": "workspace:*",
    "expo": "~57.0.11",
    "@types/jest": "^29.5.14",
    "@types/react": "~19.2.2"
  }
}
