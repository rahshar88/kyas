{
  "name": "@kyascene/domain",
  "version": "0.1.0",
  "private": true,
  "description": "KyaScene domain types, status transitions and policy constants. No I/O, no vendor SDKs.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "echo \"no tests in @kyascene/domain yet\" && exit 0",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@kyascene/config": "workspace:*",
    "@types/jest": "^29.5.14"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
